---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: wrast/ubuntu-expect

inputs:
  - name: cna-iaas-smoke-test
  - name: smoke-test

run:
  path: cna-iaas-smoke-test/ci/scp.sh
  args: [{{ host1.name }}, {{ host1.user }}, {{ host1.password }}, {{ host1.path }}]
