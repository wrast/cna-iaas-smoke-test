---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: wrast/ubuntu-expect

params:
  host-name: {{host_name}}
  host_user:
  host_password:
  host_path:

inputs:
  - name: cna-iaas-smoke-test
  - name: smoke-test

run:
  path: cna-iaas-smoke-test/ci/scp.sh
  args: [{{host_name}}, {{host_user}}, {{host_password}}, {{host_path}}]
